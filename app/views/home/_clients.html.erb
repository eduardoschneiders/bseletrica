<div class="clients mainLines">
  
  <div class="row">
    <div class="span12 application">

        <div class="title">Clientes <span><%= link_to "Veja mais", clients_path %></li></span></div>
        <ul>
          <% @clients.each do |client|%>
            <li>
              <span><%= image_tag client.image.url(:small), :align => "center" %></span>
            </li>
          <% end %>
        </ul>

    </div>
  </div>

</div>